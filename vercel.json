{
  "buildCommand": "python manage.py collectstatic --noinput",
  "installCommand": "pip install -r requirements.txt",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "bhola/wsgi.py"
    }
  ]
}